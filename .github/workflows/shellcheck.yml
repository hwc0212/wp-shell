name: Shell checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Syntax and safety checks
        run: |
          bash tests/static-checks.sh
          bash tests/config-roundtrip.sh
          bash tests/render-nginx.sh

      - name: ShellCheck
        run: >-
          docker run --rm
          -v "$PWD:/mnt:ro"
          koalaman/shellcheck:stable
          -x
          /mnt/wp-vps-manager.sh
          /mnt/deploy-single-wordpress.sh
          /mnt/tests/static-checks.sh
          /mnt/tests/config-roundtrip.sh
          /mnt/tests/render-nginx.sh
          /mnt/tests/nginx-integration.sh
          /mnt/tests/service-config-integration.sh

      - name: Nginx configuration integration test
        run: >-
          docker run --rm
          -v "$PWD:/mnt:ro"
          ubuntu:24.04
          bash -c
          'apt-get update >/dev/null &&
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends nginx openssl >/dev/null &&
          bash /mnt/tests/nginx-integration.sh'

      - name: Service configuration integration test
        run: >-
          docker run --rm
          -v "$PWD:/mnt:ro"
          ubuntu:24.04
          bash -c
          'apt-get update >/dev/null &&
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends mariadb-server redis-server php8.3-fpm openssl >/dev/null &&
          bash /mnt/tests/service-config-integration.sh'
